@model IEnumerable<QL_TourDuLich.BUS.LoaiHinhDuLich>

@{
    ViewBag.Title = "Index";
}

<h2>Loại Hình Du Lịch</h2>
@using (Html.BeginForm("Index", "LoaiHinhDuLiches", FormMethod.Get))
{
    @Html.TextBox("q")
    <input type="submit" value="Tìm" />
}
<p>
    @Html.ActionLink("Thêm mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Tên Loại Hình
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenLoaiHinh)
            </td>
            <td>
                @Html.ActionLink("Sửa", "Edit", new { id = item.MaLoaiHinh }) |
                @Html.ActionLink("Chi tiết", "Details", new { id = item.MaLoaiHinh }) |
                @Html.ActionLink("Xoá", "Delete", new { id = item.MaLoaiHinh })
            </td>
        </tr>
    }

</table>
