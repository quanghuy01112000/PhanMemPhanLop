@model IEnumerable<QL_TourDuLich.BUS.GiaTour>

@{
    ViewBag.Title = "Index";
}

<div class="row column_title">
    <div class="col-md-12">
        <div class="page_title">
            <h2>Giá Tour</h2>
        </div>
    </div>
</div>

<div class="row">

    <div class="col-md-12">
        @using (Html.BeginForm("Index", "GiaTours", FormMethod.Get))
        {
            @Html.TextBox("q")
            <input class="btn cur-p btn-secondary" type="submit" value="Tìm Giá Tour" />
        }
        <br />
    </div>

    <div class="col-md-12 ">
        <button type="button" class="btn cur-p btn-outline-primary ">@Html.ActionLink("Thêm mới", "Create")</button>

    </div>

    <div class="col-md-12 ">
        <br />

    </div>

    <div class="col-md-12">
        <div class="white_shd full margin_bottom_30">
            <div class="full graph_head">
                <div class="heading1 margin_0">
                    <h2>Danh sách giá tour</h2>
                </div>
            </div>
            <div class="table_section padding_infor_info">
                <div class="table-responsive-sm">
                    <table class="table">
                        <thead class="thead-dark">
                            <tr>
                                <th>
                                    Thành Tiền
                                </th>
                                <th>
                                    Thời Gian Bắt Đầu
                                </th>
                                <th>
                                    Thời Gian Kết Thúc
                                </th>
                                <th>
                                    Tên Tour
                                </th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {

                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.ThanhTien)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.ThoiGianBatDau)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.ThoiGianKetThuc)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.TourDuLich.TenTour)
                                </td>
                                <td>
                                    <button type="button" class="btn cur-p btn-outline-success">@Html.ActionLink("Sửa", "Edit", new { id = item.MaGia })</button> |
                                    <button type="button" class="btn cur-p btn-outline-info">@Html.ActionLink("Chi tiết", "Details", new { id = item.MaGia })</button> |
                                    <button type="button" class="btn cur-p btn-outline-warning">@Html.ActionLink("Xoá", "Delete", new { id = item.MaGia })</button>

                                </td>
                            </tr>

                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

</div>




